# Session 22: Maturation Map & Sovereign Edge Roadmap
**Date:** 2026-06-02
**Context:** This document serves as the master "To-Do" and maturation map based on the recent CosyVoice WebGPU breakthrough, the Phase 4 Sovereign Sound Architecture, and the Codebase/Implementation Audits.

## 1. Current Development State
The platform has successfully pivoted toward a **Sovereign, Edge-Native Pedagogical OS**. We have established the high-fidelity, client-side inference pipeline:
- **Audio Processing:** `useStudioAudio.js` is live with mathematical reverb and compression.
- **Edge Voice Generation:** `cosyVoiceWorker.js` and `useCosyVoice.js` provide WebGPU-accelerated TTS.
- **Recording UI:** `SomaticStudioPrompter.jsx` is built but unrouted.

**The primary conflict:** We have state-of-the-art edge AI running inside a fragmented, legacy React UI. The 144-node DAG progress system is a "ghost" (it exists in code but is not visible to the student). The tools exist in a disjointed "Developer Menu" (GuitarWorkbench) rather than an integrated learning journey.

---

## 2. The Master "To-Do" List: Scaling the Architecture

### PHASE A: Immediate Plumbing & Production (This Week)
These are the critical blockers preventing Bertrand from recording new content and completing the pedagogical loop.

- [x] **Route the Somatic Prompter:** Add `<Route path="/studio/prompter" element={<SomaticStudioPrompter />} />` to `App.jsx`.
- [x] **Cloudflare R2 Backend Scaffold:** Build the Pre-signed URL backend logic (e.g., via a lightweight Supabase edge function or dedicated worker) to handle the 4.5MB+ binary video blobs generated by the Prompter, bypassing Vercel limits.
- [ ] **Content Production (Bertrand):** Utilize the routed Prompter to record the 4 missing core videos:
  - *Active Imagination*
  - *The Pothole*
  - *SHEARL Protocol*
  - *Applied Theory*


### PHASE B: The Portal Merger (The UI Overhaul)
Transforming the scattered 4-portal system into the unified 3-portal journey (Read & Learn, Play & Grow, Watch & Connect).

- [x] **Build The Workbook (`/workbook`):**
  - Merge `GuitarWorkbench` and `PlaybookShell`.
  - Replace the 12-tool grid with the **Fret Timeline**.
  - Add the **Character Header** (Bard Level, XP) to the top.
  - Implement the **Quest Log** as the primary Call-To-Action (e.g., "⚡ Current Quest: Can I sing and play?").
  - Deprecate `/guitar` and `/playbook` routes (add redirects).
- [ ] **Build The Studio (`/studio`):**
  - Extract the video library from `PlayerPortal` to be front-and-center.
  - Keep "Record Practice for Bertrand" as the primary static action.
  - Integrate mentor feedback inline.
  - Migrate `DigitalBinder` resources into the Studio.

### PHASE C: Resurrecting the Ghost DAG
Wiring the invisible 144-node mathematical curriculum into the UI so students actually progress through the Be, Do, Play methodology.

- [ ] **Mark Complete Buttons:** Add "Mark Complete" functionality to `SlideViewer` and tool exercises so students explicitly trigger `completePhase()`.
- [x] **Sync DAG to UI:** Read `completedNodes` from the new Workbook UI to gray out locked frets and highlight the active pedagogical phase (Be, Do, or Play).
- [ ] **Identity Scaffolding:** Wire `promptVerbosity` into the `TroubadourWidget` so the AI speaks less as the student's DAG mastery increases.
- [ ] **Tier Integration:** Connect the chosen Commitment Tier (Gentle, Committed, Intensive) to `practiceEngine.js` to dynamically scale the generated practice session times.

### PHASE D: Sovereign Desktop & Native Pivot (Long-Term)
Executing the Phase 4 technical architecture for maximum performance and local-first compliance.

- [ ] **Tauri Desktop Scaffold:** Containerize the React/Vite app into a standalone desktop application via Tauri, eliminating browser storage limits and enabling local model downloads.
- [ ] **Open Notebook RAG (Java/PostgreSQL):** Stand up the `pgvector` HNSW index and LangChain4j document ingestion pipeline for semantic searching of Bertrand's textbooks.
- [ ] **Android Native Audio Runtime:** Implement the Google Oboe C++ layer for the mobile wrapper to achieve sub-15ms real-time audio monitoring latency.
